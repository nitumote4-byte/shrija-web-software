# Frontend (Vercel)
# Leave VITE_API_URL EMPTY on Vercel — /api is proxied to Railway via vercel.json
# Only set this for special cases (direct browser → API):
# VITE_API_URL=https://shrija-web-software-production.up.railway.app

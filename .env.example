# Frontend (Vercel)
# Leave VITE_API_URL empty — browser uses same-origin /api (proxied by api/[...path].js)
# Optional: Railway URL override for the serverless proxy
# RAILWAY_API_URL=https://shrija-web-software-production.up.railway.app
